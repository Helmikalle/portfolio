---
import { projects } from "@data/projects";
---

<section id="projects" class="section" aria-labelledby="projects-title">
  <div class="container">
    <div class="section-header">
      <div>
        <p class="section-label">Selected Work</p>
        <h2 id="projects-title" class="section-title">Production work across backend systems, cloud services, and AI tooling</h2>
      </div>
      <p class="section-intro">A focused view of client work and technical themes from recent professional experience.</p>
    </div>
    <div class="grid gap-4 lg:grid-cols-2">
      {
        projects.map((project) => (
          <article class="overflow-hidden rounded-lg border border-line bg-white shadow-panel">
            <div class="border-b border-line bg-cloud-strong p-4 sm:grid sm:min-h-44 sm:place-items-center sm:p-6">
              <span class="inline-flex rounded-lg border border-line bg-white px-3 py-2 text-sm font-bold text-accent shadow-sm sm:border-0 sm:bg-transparent sm:p-0 sm:text-center sm:text-base sm:text-muted sm:shadow-none">
                {project.eyebrow}
              </span>
            </div>
            <div class="p-6">
              <h3 class="card-title">{project.title}</h3>
              <p class="leading-8 text-muted">{project.description}</p>
            </div>
          </article>
        ))
      }
    </div>
  </div>
</section>
